objective 'Test Dependancy', (should) ->

    context 'Context One', ->

        it 'test one', -> 1

        it 'test two', (done) ->

            setTimeout(done, 1000);

        it 'test three',(done) -> 

            setTimeout(done, 500);

    context.only 'Context Two', ->

        it 'test four', -> 

            1.should.equal 2

        it 'test five', (done) ->

            setTimeout(done, 1000);

        it 'should fail',(done) -> 

            setTimeout (->
                done new Error 'moo'
            ), 500

        it 'test seven',(done, http) ->

            setTimeout(done, 500);

        it 'test eight',(done) -> 

            setTimeout(done, 500);

        it 'test nine',(done) -> 

            setTimeout(done, 500);
