objective 'Test Dependancy', (should) ->

    # before ->

    #     throw new Error 'Failed!'

    # after ->

    #     console.log 'still run this'

    context 'Context One', ->

        count= 0

        beforeEach (done) ->
            
            # count++
            # console.log('B count %d', count)
            done()# if count == 1

        after ->

            throw new Error 'After Failed!'
            # console.log 'still run this'

        afterEach (done) ->

            count++
            console.log('A count %d', count)
            done() # if count < 3
            # console.log 'still do this too'

        it 'test one', -> 1

        it 'test two', (done) ->

            setTimeout(done, 100);

        it 'test three',(done) -> 

            setTimeout(done, 100);

    context 'Context Two', ->

        it 'test four', -> 

            1.should.equal 2

        it 'test five', (done) ->

            setTimeout(done, 100);

        it 'should fail',(done) -> 

            setTimeout (->
                done new Error 'moo'
            ), 100

        it 'test seven',(done, http) ->

            setTimeout(done, 100);

        it 'test eight',(done) -> 

            setTimeout(done, 100);

        it 'test nine',(done) -> 

            setTimeout(done, 100);

    context 'Context Three', ->

        it 'overdoes', (done) ->

            done()
            setTimeout (->
                done()
            ), 10
            done()
