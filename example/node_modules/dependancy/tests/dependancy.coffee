objective 'Test Dependancy', (should) ->

    context.only 'Context One', ->

        before ->

            throw new Error 'Failed!'

        beforeEach ->

            console.log 'dont do this'

        afterEach ->

            console.log 'dont do this'

        after ->

            console.log 'still run this'

        it 'test one', -> 1

        it 'test two', (done) ->

            setTimeout(done, 100);

        it 'test three',(done) -> 

            setTimeout(done, 100);

    context 'Context Two', ->

        it 'test four', -> 

            1.should.equal 2

        it 'test five', (done) ->

            setTimeout(done, 100);

        it 'should fail',(done) -> 

            setTimeout (->
                done new Error 'moo'
            ), 100

        it 'test seven',(done, http) ->

            setTimeout(done, 100);

        it 'test eight',(done) -> 

            setTimeout(done, 100);

        it 'test nine',(done) -> 

            setTimeout(done, 100);

    context 'Context Three', ->

        it 'overdoes', (done) ->

            done()
            setTimeout (->
                done()
            ), 10
            done()
